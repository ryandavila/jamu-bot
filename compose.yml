services:
  jamu-bot:
    build: .
    container_name: jamu-bot
    environment:
      - DISCORD_TOKEN=${DISCORD_TOKEN}
      - JAMU_ENV=${JAMU_ENV:-prod}
    command: python bot.py
    network_mode: host
    restart: unless-stopped
